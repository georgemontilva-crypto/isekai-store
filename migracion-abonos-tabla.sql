CREATE TABLE orderPayments (id INT AUTO_INCREMENT PRIMARY KEY, orderId INT NOT NULL, amount DECIMAL(10,2) NOT NULL, method VARCHAR(50), reference VARCHAR(256), holder VARCHAR(256), receiptUrl TEXT, status VARCHAR(20) NOT NULL DEFAULT 'pending', source VARCHAR(20) NOT NULL DEFAULT 'cliente', createdAt TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP);
